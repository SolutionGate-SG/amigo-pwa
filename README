# Nepal eCommerce PWA

A multivendor eCommerce Progressive Web App for Nepal, built with Next.js, NestJS, Supabase, Hasura, and Stripe.

## Setup
1. Clone the repo: `git clone https://github.com/your-username/ecommerce-pwa.git`
2. Install dependencies: `cd frontend && npm install; cd ../backend && npm install`

## Development
- Frontend: `cd frontend && npm run dev`
- Backend: `cd backend && npm run start:dev`